<h1>Movies
  <small>
    <a class="btn btn-success pull-right" href="/movies/new">Add a New Movie</a>
  </small>
</h1>

<table class='table table-striped'>
  <thead>
    <tr>
      <th>Title</th>
      <th>Directed By</th>
      <th>Year</th>
      <th></th>
    </tr>
  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= link_to movie.title, "/movies/#{movie.id}" %></td>
        <td><%= link_to Director.find_by(:id => movie.director_id).name, "/directors/#{movie.director_id}" %> </td>
        <td><%= movie.year %></td>
        <td><%= link_to "Delete", "/movies/#{movie.id}/delete" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
